```{=html}
<%
const featured = items.filter(item => item.featured);
const others = items.filter(item => !item.featured);
%>

<div class="featured-projects">
<% for (const item of featured) { %>
  <div class="featured-project">
    <h2 class="featured-project-name"><a href="<%= item.href %>"><%= item.title %></a></h2>
    <p class="featured-project-tagline"><%= item.tagline %></p>
    <div class="featured-project-links">
      <a href="<%= item.href %>" class="featured-project-link">Learn more &rarr;</a>
      <div class="project-badges">
        <% for (const link of item["secondary-links"]) { %>
          <a href="<%= link.href %>" target="_blank" rel="noopener noreferrer" class="project-badge">
            <i class="bi bi-<%= link.icon %>"></i>
            <%= link.text %>
          </a>
        <% } %>
      </div>
    </div>
  </div>
<% } %>
</div>
```
